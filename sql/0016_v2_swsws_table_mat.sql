create table if not exists swsws_table (
	expert_name varchar(255),
	game_key varchar(255),
	expert_id int,
	affiliate varchar(255),
	link varchar(255),
	predicted_winner_name varchar(255),
	predicted_winner_id int,
	money_line int,
	score int,
	at_date int,
	rn int
);
